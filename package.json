{
  "name": "galaxy-outlast",
  "version": "1.55.0",
  "description": "Galaxy Outlast Mobile Game",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build && node scripts/sync-onedrive.cjs",
    "sync": "node scripts/sync-onedrive.cjs",
    "sync:watch": "node scripts/sync-onedrive.cjs --watch",
    "build:apk": "npx cap sync android && gradlew.bat -p android assembleRelease && node scripts/sync-onedrive.cjs",
    "preview": "vite preview",
    "test": "node scripts/verify.cjs && node scripts/test_in_headless_chrome.cjs",
    "test:browser": "node scripts/test_in_headless_chrome.cjs",
    "check": "npm run build && node scripts/verify.cjs && node scripts/test_in_headless_chrome.cjs"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.1",
    "@capacitor/core": "^7.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.1",
    "vite": "^6.4.3",
    "vite-plugin-singlefile": "^2.3.3",
    "ws": "^8.21.3"
  }
}
